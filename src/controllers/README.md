# Controllers

HTTP controller logic is grouped by API area inside src/app.js after the MVC migration scaffold. The next low-risk step is extracting each route group here without changing handler bodies.
