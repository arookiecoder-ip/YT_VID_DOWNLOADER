version: '3.8'

services:
  tubegrab:
    build: .
    container_name: tubegrab
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - downloads:/app/downloads
    restart: unless-stopped
    mem_limit: 1g

volumes:
  downloads:
